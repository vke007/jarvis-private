# ─────────────────────────────────────────────────────────────
# JARVIS Private App — Environment Variables
# Copy this to .env for local dev (never commit .env to GitHub!)
# Set these same variables inside Railway dashboard
# ─────────────────────────────────────────────────────────────

# ── Security ──────────────────────────────────────────────────
SECRET_KEY=replace-with-a-long-random-string-minimum-32-chars

# ── Your Private Login Credentials ───────────────────────────
# ONLY this email and password can log in — no registration allowed
OWNER_EMAIL=your@email.com
OWNER_PASSWORD=YourStrongPassword123!
OWNER_NAME=YourName

# ── Anthropic AI  (get from console.anthropic.com) ────────────
ANTHROPIC_API_KEY=sk-ant-your-key-here

# ── Database (Railway sets this automatically) ────────────────
# DATABASE_URL=postgresql://...  ← Railway injects this for you

# ── Server Port (Railway sets automatically) ──────────────────
# PORT=5000
